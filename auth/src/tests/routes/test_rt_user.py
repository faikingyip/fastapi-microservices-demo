# Authenticated user retrieved
# Request denied for unauthenticated user
# Invalid access token on wrong token format
# Invalid access token on expired token
# Invalid access token on wrong token type (token_type != access)
