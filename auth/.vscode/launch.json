{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": ["src.auth.entrypoints.fastapi.main:app", "--reload", "--port", "8000"],
            "cwd": "${workspaceFolder}"
        }
    ]
}